const checkName = (firstName, lastName) => {
  if (firstName !== 'Monicah' || lastName !== 'Kwamboka') {
    alert('You are not Monicah Kwamboka');
  } else {
    alert('You are Monicah Kwamboka');
  }
};

checkName('Monicah', 'Kwamboka');
